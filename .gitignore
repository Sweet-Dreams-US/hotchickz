# dependencies
node_modules

# build output
dist
dist-ssr
*.local

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# tooling scratch / reference captures (kept locally, not in repo)
.playwright-mcp/
hotchickz-current-site.png
hotchickz-menu.png
qa-*.jpeg
qa-*.png

# oversized raw source assets dropped into public/assets — kept on disk,
# excluded from the repo + Pages deploy (20-60MB camera exports / video).
# Optimize before using on the site.
public/assets/DSC*.jpg
public/assets/*.mp4
